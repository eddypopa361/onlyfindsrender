Task: rebranding cromatic + înlocuire logo, fără nicio schimbare de logică/structură.

1) Culoare primară unică:
   - primary base: #00BDFF
   - text pe primary / pe background închis: #ffffff
   - background principal rămâne ce este acum (nu schimba layout-ul), dar dacă există "accent backgrounds", folosește #00BDFF.
   - Hover/active/focus/glow/gradients trebuie refăcute pornind strict din #00BDFF (fără alte brand colors).

   Propuneri de derivate (folosește sau calculează echivalente):
   - hover:  #00A8E6  (darken ~10–12%)
   - active: #008EC4  (darken supl.)
   - subtle: #E6F9FF  (tint pentru badge-uri)
   - ring/glow: rgba(0,189,255,0.55) și rgba(0,189,255,0.35)
   - gradient exemplu: linear-gradient(135deg, #00BDFF 0%, #00A8E6 55%, #66E0FF 100%)
     + fallback solid: #00BDFF

   Ce trebuie modificat:
   - tailwind.config.* / theme tokens: remapează `primary` la #00BDFF (dacă există scale 50..950, regenerează o scară coerentă cu 500 = #00BDFF; altfel păstrează doar valorile folosite).
   - variabile CSS (ex: --color-primary, --ring, --shadow): actualizează la noile valori.
   - clase Tailwind din componente: înlocuiește accentele vechi (ex. text-[brand], from-*, via-*, to-*) cu variante bazate pe primary (text-white unde e text pe butoane, bg-primary-500, hover:bg-[hover], ring-[rgba]).
   - stiluri inline (style="color: …" / background: …): remapează la #00BDFF/derivate.
   - NU modifica semanticile success/warning/error.

2) Înlocuire logo:
   - În TOT proiectul, înlocuiește orice referință la `/public/images/onlyfinds_logo.png` cu `/public/images/onlyfinds.png`.
   - Include HTML, JSX/TSX imports, CSS/MDX/JSON, config-uri. Nu schimba alte căi.
   - Dacă assetul nou nu există, creează un TODO clar sau adaugă fișierul (dacă îl găsești în repo).

3) Fără alte schimbări:
   - Nu redenumi componente, nu schimba props, nu modifica textele (în afară de culori și path logo).
   - Păstrează layout-ul și funcționalitatea exact ca înainte.

4) Output obligatoriu:
   - Lista fișierelor modificate + diff (patch).
   - Fragment actualizat din `tailwind.config.*` (sau sistemul de teme) cu noile culori.
   - Exemple scurte: un Button și un Card cu gradient nou (doar pentru verificare vizuală).
   - Rulă o scanare finală și confirmă că nu mai există culori de accent vechi în cod (purple/indigo/fuchsia/tomato etc. dacă erau) și că toate referințele la vechiul logo au fost înlocuite.

Execută acum, te rog.
